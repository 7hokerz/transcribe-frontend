rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // By default, deny all client-side access.
    // All data mutations should go through server actions.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}